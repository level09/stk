services:

  redis:
    container_name: redis 
    image: 'redis:7-alpine'
    user: "${DOCKER_UID:-1000}"
    command: redis-server --requirepass ${REDIS_PASSWORD:-verystrongpass}
    volumes:
      - 'redis-data:/data'
    ports:
      - '6379:6379'
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 3

  postgres:
    container_name: postgres
    image: 'postgres:15-alpine'
    volumes:
      - 'postgres-data:/var/lib/postgresql/data'
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=stk
      - POSTGRES_PASSWORD=${DB_PASSWORD:-verystrongpass}
      - POSTGRES_DB=stk
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "stk"]
      interval: 5s
      timeout: 3s
      retries: 3

  website:
    container_name: website
    build: .
    user: "${DOCKER_UID:-1000}"
    volumes:
      - './stk:/app/stk'
      - './run.py:/app/run.py'
    depends_on:
      - redis
      - postgres
    ports:
      - '8000:5000'
    env_file:
      - .env
    environment:
      - QUART_APP=run.py
      - QUART_DEBUG=0
      - SQLALCHEMY_DATABASE_URI=postgresql+asyncpg://stk:${DB_PASSWORD:-verystrongpass}@postgres/stk
      - REDIS_SESSION=redis://:${REDIS_PASSWORD:-verystrongpass}@redis:6379/1
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/"]
      interval: 30s
      timeout: 10s
      retries: 3

  nginx:
    container_name: nginx
    restart: always
    image: 'nginx:alpine'
    user: "${DOCKER_UID:-1000}"
    ports:
      - '80:80'
    volumes:
      - './stk/static/:/app/static/:ro'
      - './nginx/nginx.conf:/etc/nginx/nginx.conf:ro'
      - './nginx/stk.conf:/etc/nginx/conf.d/default.conf:ro'
    tmpfs:
      - /tmp:exec
      - /var/run
      - /var/cache/nginx
    depends_on:
      - website
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 10s

volumes:
  redis-data:
    driver: local
  postgres-data:
    driver: local
